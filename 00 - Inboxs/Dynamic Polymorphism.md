- Xảy ra khi phương thức được xác định lúc chạy, không phải lúc biên dịch.
    
- Thường đạt được bằng **override** và **con trỏ/lớp cha tham chiếu lớp con**.

**Đa hình động** cho phép hành vi thay đổi linh hoạt theo đối tượng thực tế.